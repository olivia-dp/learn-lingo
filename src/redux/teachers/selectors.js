export const selectTeachers = (state) => state.teachers.items;
export const isTeachersLoading = (state) => state.teachers.loading;
export const selectLanguages = (state) => state.teachers.languages;
export const selectLevels = (state) => state.teachers.levels;
export const selectPrices = (state) => state.teachers.prices;
export const selectFilters = (state) => state.teachers.filters;
